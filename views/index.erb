<div class="row">
  <div class="col-sm-12">
    <div class="card">
      <h1>Map Will Go Here</h1>
    </div>
  </div>
</div>
<br>
<div class="row">
  <div class="col-sm-6">
    <div class="card">
      <% if @games != nil %>
        <h4>Available Games:</h4>
        <ul>
          <% @games.each do |game| %>
            <% court = Court.find(game.court_id) %>
            <li><a href="/game/<%= game.id %>"><%=game.game_name%></a><%= court.title %>, <%= game.date %>, <%= game.time %></li>
          <% end %>

        </ul>
      <% else %>
        <h4>No Available Games Yet</h4>
      <% end %>
    </div>
  </div>
  <div class="col-sm-6">
    <div class="card">
      <h4>Create Game:</h4>
      <form class="" action="/create_game" method="post">
        <div class="form-group">
          <label for="game_name">Game Name:</label>
          <input type="text" name="game_name" class="form-control">
        </div>
        <div class="form-group">
          <label for="court">Court:</label>
          <select class="form-control" name="court">
            <% if @courts != nil %>
              <% @courts.each do |court| %>
                <option value="<%= court.id %>"><%= court.title %></option>
              <% end %>
            <% else %>
              <option>There Are No Courts Yet</option>
            <% end %>
          </select>
        </div>
        <div class="form-group">
          <label for="date">Date:</label>
          <input class="form-control" type="date" name="date">
        </div>
        <div class="form-group">
          <label for="time">Time:</label>
          <input class="form-control" type="time" name="time">
        </div>
        <button class="btn btn-success" type="submit" name="button">Create Game</button>
      </form>
    </div>
  </div>
</div>
